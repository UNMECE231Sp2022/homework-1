#ifndef CUSTOM_VECT_MATH_H
#define CUSTOM_VECT_MATH_H
#include "vect.h"

void scale_vector(Vector *v, int scale_value);
int vector_sum(Vector *v);
int dot_product(Vector *a, Vector *b);

#endif // CUSTOM_VECT_MATH_H
